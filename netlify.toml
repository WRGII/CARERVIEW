# netlify.toml
[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA fallback: send ALL paths to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
